# =============================================================================
# MIDIS_ALL_REAL — track CODE + docs ONLY.
# Everything heavy (the corpus, caches, vectors, catalogs, audio, env) is
# regenerable or huge and must NEVER go to git/GitHub (100 MB/file limit).
# =============================================================================

# --- Python env / caches ---
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- Large data directories (regenerable / corpus) ---
MIDIs/
NOTESEQ_DATA/
META_DATA/
CHORDS_DATA/
SIGNATURES_DATA/
catalog/
TOKENIZED/
pools/
_work/
_stats/
_logs/
_staging_imac/
_quarantine/
soundfonts/
web/vendor/

# --- Big / binary file types (belt-and-suspenders) ---
*.mid
*.midi
*.wav
*.mp3
*.flac
*.ogg
*.m4a
*.npy
*.npz
*.pkl
*.pickle
*.parquet
*.sqlite
*.sqlite3
*.db
*.bin
*.safetensors
*.ckpt
*.pt
*.pth
*.sf2
*.sf3
*.bak
*.bak.*
*.log
*.tsv

# --- Defensive: any common data/corpus folder name, anywhere, fully ignored ---
data/
corpus/
corpora/
dataset/
datasets/
midis/
MIDIS/
midi/
raw/
raw_data/
*_data/
*_DATA/

# --- Editor / OS cruft ---
.claude/
.DS_Store
*.swp
*~

# --- Playwright/Grok bridge scratch (snapshots, console logs) ---
.playwright-mcp/
starmap_targets.png

# --- Nested SEPARATE project (its own git repo); do not vendor it here ---
music_rules/

# --- Stray top-level data/asset dirs (huge; never push to GitHub) ---
GMT/
8-bit_ALBUMS/
MIDI_VIDEO_GAME/
MIDIS_TO_BE_INJESTED/
_dailygrok_archive/
t/
